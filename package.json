{
  "name": "kira",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply money-manager-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply money-manager-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
